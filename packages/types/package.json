{
  "name": "@lex-love-loom/types",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types and Zod schemas between FE (apps/frontend) and BE (apps/backend).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./schemas": "./src/schemas/index.ts",
    "./supabase": "./src/supabase.ts"
  },
  "scripts": {
    "lint": "echo \"(types) no lint configured\" && exit 0",
    "test": "echo \"(types) no tests\" && exit 0"
  },
  "dependencies": {
    "zod": "^3.23.8"
  }
}
